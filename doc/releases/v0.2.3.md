## What changed

- Fixed macOS release tests that compared `/var` paths with their canonical
  `/private/var` paths. The macOS release build now checks the same behavior as
  Linux.
- Added a plain-language README explanation of the binary, hook wiring, and
  before-write policy flow.

## Verification

- `cargo test --locked --all-targets --all-features`: 319 passed on Linux.
- macOS release test failure reproduced and fixed by canonical-path assertions.
- `cargo clippy --locked --all-targets --all-features -- -D warnings`: passed.
- `gitleaks detect --source . --no-banner`: passed.

## Upgrade

```sh
lgtm update
```
