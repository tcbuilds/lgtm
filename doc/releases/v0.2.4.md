## What changed

- Fixed `evidence-claims-honest` false blocks for operational LGTM notes.
  Claims about `lgtm doctor` and hook probes are no longer treated as
  repository test claims.
- Repository quality-gate claims still require matching current Stop command
  evidence with exit status 0. `lgtm check --tier full` remains a verifiable
  quality claim.

## Verification

- `cargo test --locked --all-targets --all-features`: 321 passed.
- `cargo clippy --locked --all-targets --all-features -- -D warnings`: passed.
- `cargo fmt --check`: passed.
- `gitleaks detect --source . --no-banner`: passed.
- Release workflow runs the full cross-platform test and build matrix.

## Upgrade

```sh
lgtm update
```
