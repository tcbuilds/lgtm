## What changed

- Added policy authoring commands: `lgtm policy validate`, `add`, `test`, and `drift`.
- Added pinned organization policy bundles with deterministic embedded → organization → repository precedence.
- Added adapter-neutral response contracts and documented Codex/CI integration through `lgtm check --tier full`.
- Added pinned Rust CI, bounded evidence artifacts, precise GitHub annotations, and MUST-only failure handling.
- Expanded workspace discovery, policy coverage, structured command trust, coverage evidence, auth/input review, and universal language fixtures.
- Added `lgtm stats` for local evidence-only summaries of noisy rules, statuses, slow checks, and missing tools.
- Added release acceptance, dogfood, adapter, and organization-policy documentation.

## Verification

- `cargo test --locked --all-targets --all-features`: 314 passed.
- `cargo clippy --locked --all-targets --all-features -- -D warnings`: passed.
- Full `lgtm check --tier full`: zero failed results; unavailable tools remain explicitly `unverified`.
- `gitleaks detect --source . --no-banner`: no leaks found.

## Upgrade

Install or update with:

```sh
curl -fsSL https://raw.githubusercontent.com/tcbuilds/lgtm/main/scripts/install.sh | sh
```
