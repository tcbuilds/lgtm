# Internal Python Repo Dogfood Log — 2026-07-11

## Baseline

`internal-python-repo` was selected because its recent history contains more active Python agent work than `alternate-python-repo`. LGTM initialization is committed there as `6588afb`; measured command settings are committed as `bff1ec5`.

## Sessions and Findings

| Session | Result | Finding | Action |
|---|---|---|---|
| `dogfood-2026-07-11` | False block | Init selected bare `pytest` in a uv repository; command exited 2. Report also duplicated absolute and relative paths. | Fixed uv detection and report normalization in `7288cec`. |
| `dogfood-2026-07-11-r2` | Missed verification | Correct `uv run pytest` exceeded the fixed 30-second command bound and became unverified. | Added bounded per-repository command timeouts in `2a62fd6`; configured internal-python-repo for 900 seconds. |
| `dogfood-2026-07-11-r3` | Valid block | Full pytest completed with exit 1 after roughly four minutes. | Kept the block; failures belong to the dogfood repository. |

The live baseline found no legitimate waiver need. Missing Semgrep and absent pre-edit/transcript evidence were surfaced as unverified, not passed.

## Repository Test Failures

The direct `uv run pytest -q --tb=short` reproduction found eight failures:

- One venue backfill expectation.
- Four forward-odds capture and merge expectations.
- One live MLB Stats API test returning HTTP 406.
- Two model-lab doctor/preflight acceptance expectations.

These failures were not changed as part of LGTM dogfooding. On 2026-07-11, the user authorized replacing the elapsed-week gate with at least seven representative historical task replays spanning multiple commit dates. The compressed gate must still exercise all hooks and clean, failing, security, malformed-input, and long-command cases.

## Compressed Historical Replay

Seven clean parent snapshots were replayed from commits `161c0f5`, `9364c14`, `200db5a`, `7ba2d63`, `f2f905b`, `94b94af`, and `754d0ed`, spanning 2026-06-10 through 2026-06-24. Each task exercised SessionStart, UserPromptSubmit, PreToolUse, PostToolUse, and Stop; 35 changed non-deleted Python files received both edit hooks. Historical commands were replaced by deterministic `true`, so this replay isolates hook policy and does not claim historical dependency or test validation.

The first replay found 19 premature PostToolUse blocks because cumulative test rules fired before later test-file edits. Stage-aware enforcement now records those findings as Post warnings and defers hard enforcement to Stop. A clean convergence replay reduced `new-behavior-tests-required` intermediate blocks to zero. Five commits passed Stop. Two commits (`f2f905b`, `94b94af`) correctly found broad exception catches around best-effort external boundaries; this repeated legitimate-exception pressure met ADR-0003's trigger and pulled forward the audited waiver flow in ADR-0005.

Controlled cases proved that a failing command blocks, a one-second configured timeout makes `sleep 2` unverified, a planted private key is redacted and blocked, and malformed hook/config input fails safe. Missing Semgrep produced five accurate but noisy unverified lines per Stop. The internal-python-repo working-tree status hash remained `cebb95873afef07e4cada7d41387fe2c61b56127a9fb448078d7b8f6bf4a05e7` before and after both replays.
