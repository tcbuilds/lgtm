# Engineering standards moved

The source of truth now lives in the tracked templates under
`templates/claude-rules/`. Install the rule files with `lgtm init --rules-only`.

This stub remains only as a migration pointer for repositories that still mention the old filename.
